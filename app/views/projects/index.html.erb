<p style="color: green"><%= notice %></p>

<% content_for :title, "Projects" %>

<!--<h3>Projects</h3>-->



  <% if user_signed_in? && @userprojects.any? %>
  
   <h3>My Saved Projects</h3>
    <% @userprojects.each do |project| %>
   <%= render project %>
    <h4>
      <%#= link_to "Show this project", project %>
    </h4>
    <%end%>
   <% end %>
</div>
<%= link_to "New Project", new_project_path, data: { turbo_frame: "new_project_frame" } %>

<%= turbo_frame_tag "new_project_frame" do %>
  <%# Content will be loaded here %>
<% end %>
<%#=turbo_frame_tag id ="New Project", src: new_project_path%>
<%#= link_to "New Project", new_project_path%>
<%# = link_to "My Projects", "/managements" %>
<%# = link_to "My tasks", "/tasks" %>
<%#= link_to "New project", new_project_path %>