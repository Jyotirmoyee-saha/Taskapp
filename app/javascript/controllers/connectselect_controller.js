import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="connectselect"
export default class extends Controller {
  connect() {
  }

  static target =['projectselect'];
  loadproject(){
    
  }
}
